// Comparar dois números definindo o maior
def maior(a : int, b : int) : int {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

var x : int = 10;
var y : int = 20;
var r : int = 0;

set r = maior(x, y);

print "Maior:";
print r;